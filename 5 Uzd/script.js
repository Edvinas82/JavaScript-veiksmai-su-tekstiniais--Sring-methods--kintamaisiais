let Zodis = prompt("Įveskite žodį");
console.log(Zodis.endsWith(slė));
console.log(Zodis.endsWith(as));

function Zodis(slė, as) {
    if (Zodis.endsWith(slė)) {
        return true;
    }else if (Zodis.endsWith(as)) {
        return true;
    }else{
        return false;
    }
}